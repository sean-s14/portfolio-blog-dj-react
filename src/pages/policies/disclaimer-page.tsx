import {
    Typography,
} from '@mui/material';
import PageWrapper from 'src/layout/page-wrapper'

export default function DisclaimerPage() {
    return (
        <PageWrapper styling={{p: 3}}>
            <Typography>Disclaimer Page</Typography>
        </PageWrapper>
    );
}