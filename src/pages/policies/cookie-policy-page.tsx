import {
    Typography,
} from '@mui/material';
import PageWrapper from 'src/layout/page-wrapper'

export default function CookiePolicyPage() {
    return (
        <PageWrapper styling={{p: 3}}>
            <Typography>Cookie Policy Page</Typography>
        </PageWrapper>
    );
}